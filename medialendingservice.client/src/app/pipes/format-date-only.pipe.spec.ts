import { FormatDateOnlyPipe } from './format-date-only.pipe';

describe('FormatDateOnlyPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateOnlyPipe();
    expect(pipe).toBeTruthy();
  });
});
